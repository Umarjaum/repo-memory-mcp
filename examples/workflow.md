# Suggested workflow

At the beginning of a coding session, call `get_startup_context`. When a developer corrects a repeated mistake, call `remember_correction`. Search with `recall_memory` when a relevant topic comes up. Archive obsolete guidance with `forget_memory` rather than deleting it.
